version: '3.8'

services:
  backend:
    image: nvcr.io/nvidia/rapidsai/notebooks:26.04-cuda13-py3.12
    pull_policy: always
    ulimits:
      memlock: -1
      stack: 67108864
    shm_size: 1g
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
        # Precompiled rapids-singlecell wheel matching the container's CUDA 13 runtime.
        EXTRA_PIP_PACKAGES: "rapids-singlecell-cu13==0.15.2"
    ports:
      - "0.0.0.0:8888:8888"      # JupyterLab
      - "0.0.0.0:8787:8787"      # Dask dashboard
      - "0.0.0.0:8786:8786"      # Dask scheduler
    volumes:
      - /home/ubuntu:/notebooks/workshop/
    user: root
    working_dir: /notebooks/workshop/single-cell-analysis-blueprint
    entrypoint: ["/home/rapids/entrypoint.sh"]
    command: bash /notebooks/workshop/single-cell-analysis-blueprint/setup_launchable.sh
    restart: unless-stopped
